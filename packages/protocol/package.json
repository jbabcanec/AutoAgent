{
  "name": "@autoagent/protocol",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "node --watch src/index.ts",
    "lint": "node -e \"console.log('lint: pending')\"",
    "test": "node --test",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}

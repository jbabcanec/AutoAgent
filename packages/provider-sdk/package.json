{
  "name": "@autoagent/provider-sdk",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "node --watch src/index.ts",
    "lint": "node -e \"console.log('lint: pending')\"",
    "test": "tsx --test test/conformance.test.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "tsx": "^4.20.5"
  }
}
